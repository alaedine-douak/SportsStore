@model Product

<div class="card m-1 p-2 px-3">
    <h4>
        @Model.Name
        <span class="badge rouded-pill text-bg-primary" style="float: right;">
            <small>@Model.Price.ToString("c")</small>
        </span>
    </h4>
    <div class="card-text p-1">@Model.Description</div>
</div>